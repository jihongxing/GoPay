# GoPay 生产上线整改清单

基于前两轮审计结果整理，按 `P0/P1` 排序。

## P0

### 1. 先修复编译阻塞
- 修复顺序：`internal/reconciliation/reconciliation_by_app.go` -> `internal/service/config_template_service.go` -> `internal/service/refund_service_additional_test.go`
- 验收标准：`go build ./...`、`go test ./...`、`staticcheck ./...` 全部通过
- 预估工作量：1-2 人日

### 2. 统一生产部署链路
- 修复顺序：`Dockerfile` -> `docker-compose.prod.yml` -> `docker-entrypoint.sh` -> `helm/gopay/*`
- 验收标准：容器可构建、可启动、健康检查可用、生产 compose 可落地、Helm 至少能渲染出完整资源
- 预估工作量：2-3 人日

### 3. 修正支付宝回调解析
- 修复顺序：`internal/handler/webhook.go` -> `pkg/channel/alipay/provider.go` -> 相关 webhook 测试
- 验收标准：支付宝表单回调可正确验签、解析、更新订单；新增/回归测试通过
- 预估工作量：1-2 人日

### 4. 修正微信配置与退款链路
- 修复顺序：`migrations/006_config_templates.up.sql` -> `internal/admin/config_handler.go` -> `pkg/channel/wechat/provider.go`
- 验收标准：微信渠道配置字段与运行时一致；退款请求参数正确；模板创建的配置可正常初始化 provider
- 预估工作量：2-3 人日

### 5. 补强订单幂等与数据约束
- 修复顺序：`migrations/001_init_schema.up.sql` -> `internal/service/order_service.go` -> `internal/handler/checkout.go`
- 验收标准：`app_id + out_trade_no` 具备数据库级幂等约束；并发重复下单不会写入重复订单
- 预估工作量：1-2 人日

### 6. 修复生产默认安全配置
- 修复顺序：`internal/config/config.go` -> `docker-compose.prod.yml` -> `helm/gopay/values.yaml`
- 验收标准：生产环境必须显式设置 `SERVER_ENV`、`ADMIN_API_KEY`、`PUBLIC_BASE_URL`；不再允许默认不安全值进入生产
- 预估工作量：0.5-1 人日

## P1

### 7. 统一迁移体系
- 修复顺序：`internal/database/migrate.go` -> `cmd/migrate/main.go` -> `migrations/002_indexes.sql`
- 验收标准：只有一套迁移入口；所有迁移文件都能被正式执行；索引与 schema 版本一致
- 预估工作量：1-2 人日

### 8. 修复白名单与限流
- 修复顺序：`pkg/middleware/auth.go` -> `pkg/middleware/rate_limit.go` -> `pkg/middleware/local_rate_limit.go`
- 验收标准：CIDR 白名单可用；限流失败不再默认放行；`Rate` 和 `Burst` 参数真正生效
- 预估工作量：1-2 人日

### 9. 提升异步通知可靠性
- 修复顺序：`internal/service/notify_service.go` -> `internal/handler/internal.go` -> 调度/补偿任务相关代码
- 验收标准：通知不会因 worker 池满而静默丢失；待通知订单会被周期性补偿处理；失败可追踪、可重试
- 预估工作量：2-3 人日

### 10. 修正管理后台 SQL 漂移
- 修复顺序：`internal/admin/web_handler.go` -> `internal/admin/config_handler.go` -> 相关测试
- 验收标准：后台查询字段与真实 schema、枚举值完全一致；失败订单、统计、详情、审计日志均可正常查询
- 预估工作量：1-2 人日

### 11. 处理密钥保护与明文回显
- 修复顺序：`pkg/security/encryption.go` -> `internal/service/config_service.go` -> `internal/admin/config_handler.go`
- 验收标准：渠道配置中的敏感值不再以明文进入持久层或审计日志；前端/API 不回显原始密钥
- 预估工作量：2-3 人日

### 12. 修正文档与测试失真
- 修复顺序：`DEPLOYMENT_CHECKLIST.md` -> `docs/PROJECT_STATUS.md` -> `internal/*_test.go`
- 验收标准：文档与实际行为一致；关键支付/回调/部署路径都有可复现的回归测试
- 预估工作量：1-2 人日

## 建议执行顺序

1. 先做 `1-6`，把“能编译、能部署、能收单”打通。
2. 再做 `7-12`，补齐稳定性、安全性和可维护性。
3. 每完成一个 P0 项，都重新跑一轮 `go build ./...`、`go test ./...` 和一次真实链路验证。

