apiVersion: v1
kind: Service
metadata:
  name: {{ include "gopay.fullname" . }}
  labels:
    {{- include "gopay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gopay.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
